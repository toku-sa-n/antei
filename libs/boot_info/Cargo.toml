[package]
name = "boot_info"
version = "0.1.0"
edition = "2021"
license = "MIT OR Apache-2.0"

[dependencies]
aligned_ptr = "0.1.0"
uefi = { path = "../uefi" }
x86_64 = { version = "0.14.9", default-features = false }
